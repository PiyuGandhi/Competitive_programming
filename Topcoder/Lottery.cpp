#include<bits/stdc++.h>

using namespace std;

#define ll long long 

class Lottery{
    public:
    vector<string> sortByOdds(vector<string> rules)
    {
        vector<string> ans;
        
        return ans;
    }
};